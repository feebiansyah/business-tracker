"use client";import{useState}from"react";import type{SerializableUnmatched}from"@/lib/shopee-import/types";import{formatImportRupiah,importReasonLabel,paginateUnmatched}from"./view-model";
export function UnmatchedTable({rows}:{rows:SerializableUnmatched[]}){const[current,setCurrent]=useState(1);const page=paginateUnmatched(rows,current,25);if(!rows.length)return <p className="text-sm text-slate-500">Semua group berhasil dicocokkan.</p>;return <div className="space-y-3"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{["Tanggal","Tag_link2","Reason","Row Count","Commission"].map(x=><th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody className="divide-y">{page.rows.map((row,i)=><tr key={`${row.date}-${row.tagLink2}-${i}`}><td className="px-4 py-3">{row.date}</td><td className="px-4 py-3 font-medium">{row.tagLink2}</td><td className="px-4 py-3">{importReasonLabel(row.reason)}</td><td className="px-4 py-3">{row.rowCount}</td><td className="px-4 py-3">{formatImportRupiah(row.commission)}</td></tr>)}</tbody></table></div><div className="flex items-center justify-end gap-3 text-sm"><button disabled={page.page===1} onClick={()=>setCurrent(page.page-1)}>Sebelumnya</button><span>{page.page} / {page.pageCount}</span><button disabled={page.page===page.pageCount} onClick={()=>setCurrent(page.page+1)}>Berikutnya</button></div></div>}
