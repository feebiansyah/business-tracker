import type { ShopeeCommissionPreview } from "@/lib/shopee-import/types";import{formatImportDateRange,formatImportRupiah}from"./view-model";
export function PreviewSummary({preview}:{preview:ShopeeCommissionPreview}){const items=[["File",preview.originalFilename],["Periode",formatImportDateRange(preview.dateFrom,preview.dateTo)],["Rows",String(preview.csvRowCount)],["Tag / group",String(preview.tagCount)],["Matched",String(preview.matchedCount)],["Unmatched",String(preview.unmatchedCount)],["Komisi matched",formatImportRupiah(preview.matchedCommission)],["Komisi unmatched",formatImportRupiah(preview.unmatchedCommission)]];return <div className="grid min-w-0 gap-3 sm:grid-cols-2 lg:grid-cols-4">{items.map(([label,value])=><div key={label} className="min-w-0 rounded-lg border border-slate-200 p-3"><p className="text-xs font-medium uppercase text-slate-500">{label}</p><p className="mt-1 break-words font-medium text-slate-900">{value}</p></div>)}</div>}
