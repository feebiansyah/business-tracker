"use client";
import { useState } from "react"; import { useRouter } from "next/navigation"; import { Button } from "@/components/ui/button"; import { deleteAdsterraCredentialAction, saveAdsterraCredentialAction } from "@/app/shopee/[id]/adsterra-roi/actions";
export function AdsterraCredentialForm({ shopeeAccountId, connected }: { shopeeAccountId: number; connected: boolean }) { const router = useRouter(); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); async function save(data: FormData) { setBusy(true); setError(""); const result = await saveAdsterraCredentialAction(shopeeAccountId, data); setBusy(false); if (result.success) router.refresh(); else setError(result.message); } async function disconnect() { setBusy(true); const result = await deleteAdsterraCredentialAction(shopeeAccountId); setBusy(false); if (result.success) router.refresh(); else setError(result.message); } return <section className="space-y-4 rounded-xl border border-slate-200 bg-white p-4 shadow-sm sm:p-6"><div><h3 className="font-semibold">Koneksi Adsterra</h3><p className="text-sm text-slate-500">Status: {connected ? "Terhubung" : "Belum terhubung"}. API Key dienkripsi dan hanya digunakan server-side.</p></div><form action={save} className="flex flex-col gap-3 sm:flex-row"><label className="flex-1 text-sm font-medium">API Key Adsterra<input type="password" name="apiKey" required autoComplete="new-password" placeholder={connected ? "API Key baru" : "Masukkan API Key"} className="mt-1 h-9 w-full rounded-lg border border-slate-200 px-3"/></label><Button className="sm:mt-6" disabled={busy}>{connected ? "Ganti API Key" : "Simpan Koneksi"}</Button></form>{connected && <Button type="button" variant="ghost" disabled={busy} onClick={disconnect}>Putuskan Koneksi</Button>}{error && <p className="text-sm text-red-600">{error}</p>}</section>; }
